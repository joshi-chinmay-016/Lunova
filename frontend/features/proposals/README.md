# Proposals Feature (`frontend/features/proposals`)

**Owner**: Lokesh

## Responsibilities

- Proposal inquiry inbox list
- Proposal status cards and lifecycle tags (`RECEIVED`, `PROCESSING`, `ANALYZED`, etc.)
- Proposal detail metadata view (sender, timestamp, attachments)
