# Database Infrastructure (`backend/app/infrastructure/database`)

**Owner**: Lokesh

## Responsibilities

- SQLAlchemy async engine and session management.
- Declarative models and PostgreSQL table schemas.
- Alembic database migration scripts.
- `pgvector` extension setup and connection pool configuration.
